name: LNBits Extension Integration Tests

on:
  pull_request:
    branches: [ main ]

jobs:
  extension-validation:
    name: Validate Extension Files
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Validate extension structure
      run: |
        echo "🔍 Checking required extension files..."
        
        # Check required files exist
        required_files=("manifest.json" "views.py" "models.py" "crud.py" "__init__.py")
        for file in "${required_files[@]}"; do
          if [[ -f "$file" ]]; then
            echo "✅ $file exists"
          else
            echo "❌ $file missing"
            exit 1
          fi
        done
        
        # Check Python syntax
        echo "🐍 Checking Python syntax..."
        python -m py_compile *.py
        echo "✅ All Python files have valid syntax"
        
        # Check manifest.json is valid JSON
        echo "📋 Validating manifest.json..."
        python -c "import json; json.load(open('manifest.json'))"
        echo "✅ manifest.json is valid JSON"
        
        echo "🎉 Extension validation passed!"

  python-tests:
    name: Python Code Quality
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install black flake8 mypy pydantic fastapi
        
    - name: Run code quality checks
      run: |
        echo "🎨 Running Black formatter check..."
        black --check --diff *.py || echo "Black formatting issues found"
        
        echo "🔍 Running Flake8 linter..."
        flake8 *.py --max-line-length=100 --ignore=E203,W503 || echo "Flake8 issues found"
        
        echo "🔬 Running basic import checks..."
        python -c "
        try:
            import crud, models, views
            print('✅ All modules import successfully')
        except Exception as e:
            print(f'⚠️ Import check skipped: {e}')
        "

  integration-tests:
    name: LNBits Integration Tests
    runs-on: ubuntu-latest
    needs: [extension-validation, python-tests]

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Start LNBits with Docker
      run: |
        echo "🐳 Starting LNBits with Docker (like black-panther setup)..."
        
        # Start LNBits container exactly like the working setup
        docker run -d \
          --name lnbits \
          -p 5001:5000 \
          -e LNBITS_BACKEND_WALLET_CLASS=FakeWallet \
          -e LNBITS_DATA_FOLDER=/data \
          -e LNBITS_ADMIN_UI=true \
          -e LNBITS_ALLOWED_USERS_ONLY=false \
          lnbits/lnbits:latest
        
        echo "🔄 Waiting for LNBits to be ready..."
        for i in {1..30}; do
          if curl -f http://localhost:5001/ --max-time 3 2>/dev/null; then
            echo "✅ LNBits is ready!"
            break
          fi
          echo "Waiting for LNBits... ($i/30)"
          sleep 2
        done
        
        # Final check
        if ! curl -f http://localhost:5001/ --max-time 5; then
          echo "❌ LNBits Docker container failed to start"
          echo "📋 Container logs:"
          docker logs lnbits
          exit 1
        fi
        
    - name: Install allowance extension
      run: |
        echo "📦 Installing allowance extension to Docker container..."
        
        # Copy extension files to the running container
        docker cp . lnbits:/app/lnbits/extensions/allowance/
        
        echo "✅ Extension copied to container"
        echo "📋 Checking copied files:"
        docker exec lnbits ls -la /app/lnbits/extensions/allowance/
        
        # Restart container to load the new extension
        echo "🔄 Restarting LNBits to load extension..."
        docker restart lnbits
        
        # Wait for container to be ready again
        echo "⏳ Waiting for LNBits to restart..."
        for i in {1..30}; do
          if curl -f http://localhost:5001/ --max-time 3 2>/dev/null; then
            echo "✅ LNBits is ready after restart!"
            break
          fi
          echo "Waiting for restart... ($i/30)"
          sleep 2
        done
        
    - name: Install Playwright
      run: |
        npm init -y
        npm install playwright
        npx playwright install chromium
        
    - name: Create admin account
      run: |
        cd tests
        node create-admin-account.js
        
    - name: Enable allowance extension
      run: |
        cd tests
        node enable-allowance.js
        
    - name: Test allowance creation (CRUD)
      run: |
        cd tests
        node create-allowance.js "Test Daily" 50 daily
        node create-allowance.js "Test Weekly" 100 weekly
        node create-allowance.js "Test Monthly" 200 monthly
        
    - name: Test allowance editing
      run: |
        cd tests
        node edit-allowance.js "Test Daily" "Updated Daily" 75 weekly
        
    - name: Test allowance deletion
      run: |
        cd tests
        node delete-allowance.js "Updated Daily"
        
    - name: Test API endpoints
      run: |
        cd tests
        echo "Testing API endpoints with curl..."
        
        # Test health check
        curl -f http://localhost:5001/api/v1/allowance || echo "Health check failed (expected - needs auth)"
        
        # Test API documentation endpoint
        curl -f http://localhost:5001/docs || echo "API docs endpoint check"
        
    - name: Upload test screenshots
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: integration-test-screenshots
        path: tests/test-results/
        
    - name: Stop LNBits
      if: always()
      run: |
        echo "🛑 Stopping LNBits Docker container..."
        docker stop lnbits || true
        docker rm lnbits || true